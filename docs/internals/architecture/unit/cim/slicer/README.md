# Slicer

How the value-decomposition layer is built.

- [base](base.md) — the `Slicer` ABC observable surface, the minimal-constructor contract, and the three concrete members: `DirectSlicer` (identity), `SerialSlicer` (bit-serial activations), `SimpleSlicer` (weights).
