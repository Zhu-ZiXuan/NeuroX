# Xbar Value-Domain Mapping

The mapping math specific to carrying a tensor onto an xbar's primitive cell grid.

- [slicer/](slicer/README.md) — the slicer family that decomposes an integer tensor into per-slice digit slots `[slice_num, digit_num]`, with the positional slice radix and slice weights that recombine them.
