# Slicer

The value-decomposition layer: turn an integer tensor into per-slice digit slots sized to an xbar's primitive cell grid.

- [slicer](slicer.md) — the decomposition into `[slice_num, digit_num]`, the per-slice positional radix and weights, and the two concrete decompositions (the serial activation path and the direct weight-decomposition path).
