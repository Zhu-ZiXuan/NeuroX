# Transcoder

The signed-digit encoding layer: integer-to-digit-string math, one encoding policy per representable value envelope.

- [encodings](encodings.md) — the positional radix model, the three encoding policies (true-form, radix-complement, canonical), their representable value ranges, and the shared positional decode.
