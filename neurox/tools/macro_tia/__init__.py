"""Forward TIA design tool: grid-search TIA parameters against a workload.

See ``optimize.py`` for the single CLI entry point; configuration is
fully driven by an external TOML file (no chip preset is loaded, the
design tool runs upstream of chip assembly).
"""
