# CIM evaluation status

`model.toml` disables CIM evaluation for this FP32 example. An explicitly
quantized model and its hardware configurations are required before enabling it.
No integer precision is inferred from floating operands.
