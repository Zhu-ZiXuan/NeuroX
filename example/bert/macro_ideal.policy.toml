# Nonideality policy for the BERT ideal-xbar macro.
#
# The ideal xbar carries no physical nonidealities, so its policy is the
# empty IdealCimMacroPolicy wrapped in the macro policy.

[policy]
_neurox_type = "InterArraySliceCimUnitPolicy"

[policy.cim_macro]
_neurox_type = "IdealCimMacroPolicy"
